<Window x:Class="WindowWorkspaceRestorer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Window Workspace Restorer v4" Icon="app.ico"
        Width="1160" Height="780" MinWidth="980" MinHeight="660"
        WindowStartupLocation="CenterScreen" WindowStyle="None" ResizeMode="CanResize"
        Background="#F7F8FB" FontFamily="Segoe UI, Microsoft YaHei UI" FontSize="13"
        UseLayoutRounding="True" SnapsToDevicePixels="True">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="62" ResizeBorderThickness="6" CornerRadius="12" GlassFrameThickness="0" UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>
    <Border BorderBrush="#D8DCE5" BorderThickness="1" CornerRadius="12">
        <Grid>
            <Grid.ColumnDefinitions><ColumnDefinition Width="282" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
            <Border Background="#EFF1F6" CornerRadius="12,0,0,12" BorderBrush="#E1E4EB" BorderThickness="0,0,1,0">
                <Grid Margin="20,0,20,18">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="62" /><RowDefinition Height="Auto" /><RowDefinition Height="*" />
                        <RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" shell:WindowChrome.IsHitTestVisibleInChrome="True">
                        <Button x:Name="CloseWindowButton" Style="{StaticResource TrafficLight}" Background="#FF6058" Content="×" ToolTip="关闭窗口" AutomationProperties.Name="关闭窗口" Click="Close_Click" />
                        <Button Style="{StaticResource TrafficLight}" Background="#FFBD2E" Content="−" ToolTip="最小化" AutomationProperties.Name="最小化" Click="Minimize_Click" />
                        <Button Style="{StaticResource TrafficLight}" Background="#28C840" Content="+" Foreground="#145521" ToolTip="最大化 / 还原" AutomationProperties.Name="最大化或还原" Click="Maximize_Click" />
                    </StackPanel>
                    <StackPanel Grid.Row="1" Margin="6,7,0,24">
                        <TextBlock Text="Workspace" FontSize="27" FontWeight="SemiBold" />
                        <TextBlock Text="留住工作现场，随时继续。" Foreground="{StaticResource Muted}" Margin="0,7,0,20" />
                        <DockPanel>
                            <TextBlock Text="我的工作区" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource Muted}" />
                            <TextBlock Text="{Binding Records.Count}" HorizontalAlignment="Right" Foreground="{StaticResource Muted}" />
                        </DockPanel>
                    </StackPanel>
                    <Grid Grid.Row="2">
                        <ListBox ItemsSource="{Binding Records}" SelectedItem="{Binding SelectedRecord, Mode=TwoWay}" AutomationProperties.Name="已保存工作区">
                            <ListBox.ItemTemplate><DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" ToolTip="{Binding Name}" />
                                    <TextBlock Text="{Binding Items.Count, StringFormat={}{0} 个窗口 / 标签页}" Foreground="{StaticResource Muted}" FontSize="11" Margin="0,6,0,0" />
                                    <TextBlock Text="{Binding UpdatedAt, StringFormat={}{0:MM月dd日 HH:mm}}" Foreground="{StaticResource Muted}" FontSize="11" Margin="0,3,0,0" />
                                </StackPanel>
                            </DataTemplate></ListBox.ItemTemplate>
                        </ListBox>
                        <TextBlock Text="还没有保存的工作区" Foreground="{StaticResource Muted}" HorizontalAlignment="Center" Margin="0,28,0,0" IsHitTestVisible="False">
                            <TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Visibility" Value="Collapsed" /><Style.Triggers><DataTrigger Binding="{Binding Records.Count}" Value="0"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers></Style></TextBlock.Style>
                        </TextBlock>
                    </Grid>
                    <StackPanel Grid.Row="3" Margin="0,14,0,20">
                        <TextBox Text="{Binding SelectedRecordName, UpdateSourceTrigger=PropertyChanged}" ToolTip="选中工作区的名称" AutomationProperties.Name="工作区名称" />
                        <Grid Margin="0,5,0,9"><Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition /></Grid.ColumnDefinitions>
                            <Button Command="{Binding RenameCommand}" Content="重命名" Style="{StaticResource QuietButton}" />
                            <Button Grid.Column="1" Command="{Binding DeleteCommand}" Content="删除" Style="{StaticResource QuietButton}" />
                        </Grid>
                        <Button Command="{Binding RestoreCommand}" Content="恢复所选内容" Style="{StaticResource PrimaryButton}" />
                    </StackPanel>
                    <StackPanel Grid.Row="4">
                        <Border Height="1" Background="#DDE1E9" Margin="0,0,0,18" />
                        <CheckBox x:Name="CloseToTrayToggle" Style="{StaticResource Toggle}" Content="关闭窗口时收起到托盘" IsChecked="{Binding CloseToTray, Mode=TwoWay}" FontSize="12" Margin="0,0,0,15" />
                        <CheckBox Style="{StaticResource Toggle}" Content="登录 Windows 后自动启动" IsChecked="{Binding IsStartupEnabled, Mode=TwoWay}" FontSize="12" />
                        <DockPanel Margin="0,15,0,0">
                            <TextBlock Text="VERSION 4" FontSize="10" Foreground="{StaticResource Muted}" VerticalAlignment="Center" />
                            <Button x:Name="ExitApplicationButton" Content="退出程序" Click="Exit_Click" Style="{StaticResource QuietButton}" HorizontalAlignment="Right" FontSize="11" />
                        </DockPanel>
                    </StackPanel>
                </Grid>
            </Border>
            <Grid Grid.Column="1" Margin="26,0,26,18">
                <Grid.RowDefinitions><RowDefinition Height="62" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                <DockPanel VerticalAlignment="Center">
                    <TextBlock Text="工作区概览" Foreground="{StaticResource Muted}" VerticalAlignment="Center" />
                    <Border Background="#E8EDF7" CornerRadius="7" Padding="9,4" HorizontalAlignment="Right"><TextBlock Text="本地记录" Foreground="#6D7A93" FontSize="11" /></Border>
                </DockPanel>
                <Grid Grid.Row="1" Margin="0,10,0,22">
                    <Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                    <StackPanel><TextBlock Text="从这里，继续工作。" FontSize="26" FontWeight="SemiBold" /><TextBlock Text="扫描正在打开的窗口，将它们保存为一个工作区。" Foreground="{StaticResource Muted}" Margin="0,8,0,0" /></StackPanel>
                    <Button Grid.Column="1" Command="{Binding ScanCommand}" Content="↻  扫描窗口" VerticalAlignment="Center" Margin="12,0,0,0" />
                </Grid>
                <Border Grid.Row="2" Background="White" BorderBrush="#E4E7EF" BorderThickness="1" CornerRadius="13" Padding="12">
                    <TabControl SelectedIndex="{Binding WorkspacePanelIndex, Mode=TwoWay}" Background="Transparent" BorderThickness="0">
                        <TabControl.Resources>
                            <Style TargetType="TabItem">
                                <Setter Property="Foreground" Value="{StaticResource Muted}" />
                                <Setter Property="FontWeight" Value="SemiBold" />
                                <Setter Property="Padding" Value="16,8" />
                                <Setter Property="Margin" Value="0,0,6,8" />
                                <Style.Triggers><Trigger Property="IsSelected" Value="True"><Setter Property="Foreground" Value="{StaticResource Accent}" /></Trigger></Style.Triggers>
                            </Style>
                        </TabControl.Resources>
                        <TabItem Header="当前窗口">
                            <Grid Margin="6,8,6,6">
                                <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                                <DockPanel Margin="2,0,2,17">
                                    <TextBlock Text="扫描结果" FontSize="16" FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding Candidates.Count, StringFormat={}共 {0} 项}" Foreground="{StaticResource Muted}" HorizontalAlignment="Right" />
                                </DockPanel>
                                <Grid Grid.Row="1">
                                    <ListBox ItemsSource="{Binding Candidates}" AutomationProperties.Name="扫描结果" Padding="0">
                                        <ListBox.ItemTemplate><DataTemplate>
                                            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="28" /><ColumnDefinition Width="*" /><ColumnDefinition Width="88" /></Grid.ColumnDefinitions>
                                                <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding CanPersist}" VerticalAlignment="Top" Margin="0,3,0,0" AutomationProperties.Name="{Binding Item.DisplayName}" />
                                                <StackPanel Grid.Column="1" Margin="0,0,12,0">
                                                    <TextBlock Text="{Binding Item.DisplayName}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" ToolTip="{Binding Item.DisplayName}" />
                                                    <TextBlock Text="{Binding Item.Path}" Foreground="{StaticResource Muted}" FontSize="11" TextTrimming="CharacterEllipsis" ToolTip="{Binding Item.Path}" Margin="0,5,0,0" />
                                                    <TextBlock Text="{Binding Warning}" Foreground="#A46C22" FontSize="11" TextWrapping="Wrap" />
                                                </StackPanel>
                                                <Border Grid.Column="2" Background="#F0F3F9" CornerRadius="5" Padding="5,4" VerticalAlignment="Top"><TextBlock Text="{Binding TypeLabel}" FontSize="10" Foreground="#68778F" HorizontalAlignment="Center" /></Border>
                                            </Grid>
                                        </DataTemplate></ListBox.ItemTemplate>
                                    </ListBox>
                                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" IsHitTestVisible="False">
                                        <StackPanel.Style><Style TargetType="StackPanel"><Setter Property="Visibility" Value="Collapsed" /><Style.Triggers><DataTrigger Binding="{Binding Candidates.Count}" Value="0"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers></Style></StackPanel.Style>
                                        <Border Width="58" Height="48" CornerRadius="12" Background="#EDF3FF" Margin="0,0,0,15"><TextBlock Text="▤" FontSize="30" Foreground="{StaticResource Accent}" HorizontalAlignment="Center" VerticalAlignment="Center" /></Border>
                                        <TextBlock Text="把眼前的工作，保存下来" FontSize="15" FontWeight="SemiBold" HorizontalAlignment="Center" />
                                        <TextBlock Text="点击“扫描窗口”，然后勾选需要保存的项目。" Foreground="{StaticResource Muted}" FontSize="12" Margin="0,8,0,5" />
                                        <TextBlock Text="Office · WPS 多标签 · 文件夹 · COMSOL" Foreground="#9AA2AF" FontSize="11" HorizontalAlignment="Center" />
                                    </StackPanel>
                                </Grid>
                                <Grid Grid.Row="2" Margin="0,16,0,0"><Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                                    <Grid>
                                        <TextBox Text="{Binding NewRecordName, UpdateSourceTrigger=PropertyChanged}" AutomationProperties.Name="新工作区名称" ToolTip="名称留空时自动使用保存时间" />
                                        <TextBlock Text="工作区名称（可选）" Foreground="#9AA2AF" Margin="12,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False">
                                            <TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Visibility" Value="Collapsed" /><Style.Triggers><DataTrigger Binding="{Binding NewRecordName}" Value=""><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers></Style></TextBlock.Style>
                                        </TextBlock>
                                    </Grid>
                                    <Button Grid.Column="1" Command="{Binding SaveCommand}" Content="保存工作区" Style="{StaticResource PrimaryButton}" Margin="10,0,0,0" />
                                </Grid>
                            </Grid>
                        </TabItem>
                        <TabItem Header="恢复内容">
                            <Grid Margin="6,8,6,6">
                                <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                                <Grid Margin="2,0,2,14"><Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="Auto" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{Binding SelectedRecord.Name, FallbackValue=选择一个工作区}" FontSize="16" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                                        <TextBlock Text="{Binding RestoreSelectionSummary}" Foreground="{StaticResource Muted}" FontSize="11" Margin="0,5,0,0" />
                                    </StackPanel>
                                    <Button Grid.Column="1" Command="{Binding SelectAllRestoreItemsCommand}" Content="全选" Style="{StaticResource QuietButton}" Padding="12,7" Margin="8,0,0,0" />
                                    <Button Grid.Column="2" Command="{Binding ClearRestoreItemsCommand}" Content="清空" Style="{StaticResource QuietButton}" Padding="12,7" Margin="6,0,0,0" />
                                </Grid>
                                <Grid Grid.Row="1">
                                    <ListBox ItemsSource="{Binding RestoreSelections}" AutomationProperties.Name="要恢复的内容" Padding="0">
                                        <ListBox.ItemTemplate><DataTemplate>
                                            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="28" /><ColumnDefinition Width="*" /><ColumnDefinition Width="88" /></Grid.ColumnDefinitions>
                                                <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Top" Margin="0,3,0,0" AutomationProperties.Name="{Binding Item.DisplayName}" />
                                                <StackPanel Grid.Column="1" Margin="0,0,12,0">
                                                    <TextBlock Text="{Binding Item.DisplayName}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" ToolTip="{Binding Item.DisplayName}" />
                                                    <TextBlock Text="{Binding Item.Path}" Foreground="{StaticResource Muted}" FontSize="11" TextTrimming="CharacterEllipsis" ToolTip="{Binding Item.Path}" Margin="0,5,0,0" />
                                                </StackPanel>
                                                <Border Grid.Column="2" Background="#EDF3FF" CornerRadius="5" Padding="5,4" VerticalAlignment="Top"><TextBlock Text="{Binding TypeLabel}" FontSize="10" Foreground="#5571A7" HorizontalAlignment="Center" /></Border>
                                            </Grid>
                                        </DataTemplate></ListBox.ItemTemplate>
                                    </ListBox>
                                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" IsHitTestVisible="False">
                                        <StackPanel.Style><Style TargetType="StackPanel"><Setter Property="Visibility" Value="Collapsed" /><Style.Triggers><DataTrigger Binding="{Binding RestoreSelections.Count}" Value="0"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers></Style></StackPanel.Style>
                                        <TextBlock Text="请从左侧选择一个已保存的工作区" Foreground="{StaticResource Muted}" FontSize="13" />
                                    </StackPanel>
                                </Grid>
                                <Grid Grid.Row="2" Margin="0,16,0,0"><Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                                    <TextBlock Text="只会恢复已勾选的内容" Foreground="{StaticResource Muted}" FontSize="11" VerticalAlignment="Center" />
                                    <Button Grid.Column="1" Command="{Binding RestoreCommand}" Content="恢复所选内容" Style="{StaticResource PrimaryButton}" Margin="10,0,0,0" />
                                </Grid>
                            </Grid>
                        </TabItem>
                    </TabControl>
                </Border>
                <Border Grid.Row="3" Background="White" BorderBrush="#E4E7EF" BorderThickness="1" CornerRadius="12" Padding="16,12" Margin="0,16,0,0">
                    <Expander Header="恢复详情" IsExpanded="True" Foreground="{StaticResource Muted}">
                        <TextBox Text="{Binding RestoreDetails, Mode=OneWay}" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="65" MaxHeight="130" BorderThickness="0" Background="Transparent" Padding="0,8,0,0" VerticalScrollBarVisibility="Auto" FontSize="12" />
                    </Expander>
                </Border>
                <Grid Grid.Row="4" Margin="2,15,2,0"><Grid.ColumnDefinitions><ColumnDefinition Width="15" /><ColumnDefinition /></Grid.ColumnDefinitions>
                    <Ellipse Width="6" Height="6" Fill="#34B583" VerticalAlignment="Center" HorizontalAlignment="Left" />
                    <TextBlock Grid.Column="1" Text="{Binding StatusText}" Foreground="{StaticResource Muted}" FontSize="11" TextWrapping="Wrap" />
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
